{
  "title": "Aquarius Plant LED",
  "config": {
    "step": {
      "user": {
        "title": "Connect an Aquarius Plant LED controller",
        "description": "Enter the controller's local address to read its six channel levels and current mode. For a validated controller, adjusting any channel enters and saves Manual, pausing the automatic program. Select Automatic program to resume the existing schedule. Setup and startup only read state. Other controller profiles remain read-only.",
        "data": {
          "host": "IP address or hostname",
          "port": "TCP port"
        }
      },
      "reconfigure": {
        "title": "Change controller address",
        "description": "Update this controller's address while preserving its device, entities, and names. Use the address of the same lamp: the protocol does not expose a verified immutable device identifier. The connection check only reads state.",
        "data": {
          "host": "IP address or hostname",
          "port": "TCP port"
        }
      }
    },
    "error": {
      "invalid_host": "Enter an IP address or hostname without a URL, path, or port.",
      "cannot_connect": "Could not read the controller. Check its address and connectivity from Home Assistant.",
      "invalid_response": "The controller returned an unsupported or invalid protocol reply. No settings were changed."
    },
    "abort": {
      "already_configured": "This controller address and port are already configured.",
      "already_in_progress": "A connection check for this controller is already in progress.",
      "reconfigure_successful": "Controller address updated. Device and entity identities were preserved."
    }
  },
  "entity": {
    "sensor": {
      "write_support": {
        "name": "Write support",
        "state": {
          "read_only": "Read-only: controller profile not validated",
          "validated_profile": "Controller profile validated for Manual and Automatic"
        }
      }
    },
    "select": {
      "operating_mode": {
        "name": "Operating mode",
        "state": {
          "manual": "Manual",
          "automatic_program": "Automatic program"
        }
      }
    }
  }
}
